# Go Project

## Quick start

`./run`

## Project Structure

- `main.go` - Main application entry point
- `go.mod` - Go module definition
- `flake.nix` - Optional Nix flake for development environment
